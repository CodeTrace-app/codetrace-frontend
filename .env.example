# 백엔드 API 주소
# 로컬 백엔드를 띄웠다면 아래 값을 쓰고,
# 백엔드 설치 없이 프론트만 개발한다면 배포 주소를 쓴다.
#   https://codetrace-backend-hq4u.onrender.com
VITE_API_URL=http://localhost:8000

# true면 목데이터로 동작한다 (백엔드를 띄우지 않아도 화면이 다 뜬다).
# 화면 하나씩 실제 API로 넘길 때 이 값을 false로 두고 개발한다.
VITE_USE_MOCK=true
